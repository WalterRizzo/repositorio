name = "expense-tharsis-worker"
type = "javascript"
main = "src/worker/index.ts"
compatibility_date = "2025-11-26"

[vars]
# Add any default non-secret variables here. Secrets should be stored in Worker secrets or GitHub Secrets.
# Example: DB_NAME = "expense_db"

[env.production]
# Provide an account_id in CI / secrets, or set it here for development
account_id = "$CF_ACCOUNT_ID"
name = "expense-tharsis-worker"
